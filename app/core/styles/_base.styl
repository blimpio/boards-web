html,
body {
  height: 100%;
  overflow: hidden;
  width: 100%;
}

a,
input,
button {
  outline: none !important;
}

a {
  color: #CACACA;

  &:hover,
  &:focus,
  &:active {
    color: #393939;
    text-decoration: none;
  }
}

textarea {
  resize: vertical;
}

[data-error] {
  display: none;
}
