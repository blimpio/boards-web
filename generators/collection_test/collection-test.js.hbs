describe('{{#camelize}}{{pluralName}}{{/camelize}}Collection', function() {
  var {{#camelize}}{{pluralName}}{{/camelize}}Collection = require('collections/{{pluralName}}');

  describe('when instantiated.', function() {
    var {{camelizeDashed pluralName}}Collection;

    before(function() {
      {{camelizeDashed pluralName}}Collection = new {{#camelize}}{{pluralName}}{{/camelize}}Collection();
    });

    it('should exist.', function() {
      expect({{camelizeDashed pluralName}}Collection).to.exist;
    });

    after(function() {
      {{camelizeDashed pluralName}}Collection.unplug();
    });
  });
});
