{
  "files": [{
    "from": "collection.js.hbs",
    "to": "app/collections/{{pluralName}}.js"
  }],

  "dependencies": [{
    "name": "collection_test",
    "params": "{{pluralName}}"
  }, {
    "name": "model",
    "params": "{{name}}"
  }]
}
