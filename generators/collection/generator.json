{
    "files": [{
        "from": "collection.coffee.hbs",
        "to": "app/collections/{{pluralName}}.js"
    }],
    "dependencies": [{
        "name": "collection_test",
        "params": "{{pluralName}}"
    }]
}
