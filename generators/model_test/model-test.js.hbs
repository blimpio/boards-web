describe('{{#camelize}}{{name}}{{/camelize}}Model', function() {
  var {{#camelize}}{{name}}{{/camelize}}Model = require 'models/{{name}}';

  beforeEach(function() {
    this.model = new {{#camelize}}{{name}}{{/camelize}}Model();

    it('should exist.', function() {
      expect(this.model).to.be.ok;
    });
  });
});
