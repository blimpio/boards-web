describe('{{#camelize}}{{name}}{{/camelize}}Model', function() {
  var {{#camelize}}{{name}}{{/camelize}}Model = require('model/{{name}}');

  describe('when instantiated.', function() {
    var {{camelizeDashed name}}Model;

    before(function() {
      {{camelizeDashed name}}Model = new {{#camelize}}{{name}}{{/camelize}}Model();
    });

    it('should exist.', function() {
      expect({{camelizeDashed name}}Model).to.exist;
    });

    after(function() {
      {{camelizeDashed name}}Model.unplug();
    });
  });
});
