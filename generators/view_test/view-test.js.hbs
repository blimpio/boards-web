describe('{{#camelize}}{{name}}{{/camelize}}View', function() {
  var {{#camelize}}{{name}}{{/camelize}}View = require('views/{{name}}');

  describe('when instantiated.', function() {
    var {{camelizeDashed name}}View;

    before(function() {
      {{camelizeDashed name}}View = new {{#camelize}}{{name}}{{/camelize}}View();
    });

    it('should exist.', function() {
      expect({{camelizeDashed name}}View).to.exist;
    });

    after(function() {
      {{camelizeDashed name}}View.unplug();
    });
  });
});
