describe('{{#camelize}}{{name}}{{/camelize}}View', function() {
  var {{#camelize}}{{name}}{{/camelize}}View = require('views/{{name}}');

  beforeEach(function() {
    this.view = new {{#camelize}}{{name}}{{/camelize}}View();
  });


  it('should exist.', function() {
    expect(this.view).to.exist;
  });
});
